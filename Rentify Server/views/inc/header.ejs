<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trang với Header </title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        header {
            background-color: #f28d4e;
            color: #ddd;
            padding: 15px 0;
            display: flex;
            justify-content: space-between;
            /* Điều chỉnh cho không gian giữa logo và nav */
            align-items: center;
            flex-wrap: wrap;
        }

        .header-logo {
            font-size: 24px;
            color: #fff;
        }

        .header-nav {
            list-style-type: none;
            padding: 0;
            margin: 0;
            display: flex;
            gap: 20px;
        }

        .header-nav a {
            color: #ddd;
            text-decoration: none;
            font-size: 25px;
            transition: color 0.3s;
        }

        .header-nav a:hover {
            color: #1abc9c;
        }

        .search-container {
            display: flex;
            align-items: center;
        }

        .search-container input[type="text"] {
            padding: 5px;
            font-size: 16px;
        }

        .search-container button {
            padding: 5px 10px;
            font-size: 16px;
            background-color: #1abc9c;
            color: #fff;
            border: none;
            cursor: pointer;
            margin-left: 5px;
        }

        .search-container button:hover {
            background-color: #16a085;
        }

        .social-icons {
            display: flex;
            gap: 15px;
        }

        .social-icons a {
            color: #ddd;
            font-size: 20px;
            text-decoration: none;
            transition: color 0.3s;
        }

        .social-icons a:hover {
            color: #1abc9c;
        }
    </style>
</head>

<body>

    <!-- Header -->
    <header>
        <div class="header-logo">
            <img src="/public/image/logo.png" height="100px" width="100px" />
        </div>

        <ul class="header-nav">
            <li><a href="/api/post/list">Bài đăng</a></li>
            <li><a href="/api/user/list">Người dùng</a></li>
            <li><a href="/api/staff/rooms/list">Danh sách phòng</a></li>
            <li><a href="/api/admin/login">Đăng xuất</a></li>
        </ul>

        <div class="search-container">
            <form action="/api/search" method="GET">
                <input type="text" name="query" placeholder="Tìm kiếm..." required>
                <button type="submit">Tìm</button>
            </form>
        </div>

    </header>

</body>

</html>