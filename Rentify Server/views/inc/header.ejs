<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trang với Header</title>
    <style>
        /* Các kiểu cơ bản */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        header {
            background-color: #f28d4e;
            color: #ddd;
            padding: 15px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
        }

        .header-logo img {
            height: 100px;
            width: 100px;
        }

        .header-nav {
            list-style-type: none;
            padding: 0;
            margin: 0;
            display: flex;
            gap: 20px;
        }

        .header-nav a {
            color: #ddd;
            text-decoration: none;
            font-size: 25px;
            transition: color 0.3s;
        }

        .header-nav a:hover {
            color: #1abc9c;
        }

        /* Thêm màu đỏ cho liên kết hiện tại */
        .header-nav a.active {
            color: red;
        }

        .search-container {
            display: flex;
            align-items: center;
        }

        .search-container input[type="text"] {
            padding: 5px;
            font-size: 16px;
        }

        .search-container button {
            padding: 5px 10px;
            font-size: 16px;
            background-color: #1abc9c;
            color: #fff;
            border: none;
            cursor: pointer;
            margin-left: 5px;
        }

        .search-container button:hover {
            background-color: #16a085;
        }

        .social-icons {
            display: flex;
            gap: 15px;
        }

        .social-icons a {
            color: #ddd;
            font-size: 20px;
            text-decoration: none;
            transition: color 0.3s;
        }

        .social-icons a:hover {
            color: #1abc9c;
        }
    </style>
</head>

<body>

    <!-- Header -->
    <header>
        <div class="header-logo" aria-label="Logo">
            <a href="/api/post/list">
                <img src="/public/image/logo.png" alt="Logo" />
            </a>
        </div>

        <nav aria-label="Main Navigation">
            <ul class="header-nav">
                <li><a href="/api/post/list">Bài đăng</a></li>
                <li><a href="/api/user/list">Người dùng</a></li>
                <li><a href="/api/staff/rooms/list">Phòng</a></li>
                <li><a href="/api/api/services">Dịch vụ</a></li>
                <li><a href="/api/admin/login">Đăng xuất</a></li>
            </ul>
        </nav>

        <div class="search-container" aria-label="Search Form">
            <form action="/api/search" method="GET">
                <input type="text" name="query" placeholder="Tìm kiếm..." required>
                <button type="submit">Tìm</button>
            </form>
        </div>
    </header>

    <script>
        // JavaScript để đổi màu liên kết hiện tại
        document.querySelectorAll('.header-nav a').forEach(link => {
            if (link.href === window.location.href) {
                link.classList.add('active');
            }
        });
    </script>

</body>

</html>