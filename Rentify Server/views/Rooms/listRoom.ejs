<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh sách phòng</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>

<body>
    <%- include('../inc/header') %>

        <h2 class="text-center" style="color: rgb(254, 177, 110);">Danh sách bài đăng</h2>

        <div class="container mb-4">
            <div class="d-flex justify-content-end mb-3">
                <a href="/api/post/add" class="btn btn-danger">Thêm bài đăng</a>
            </div>

            <div class="row">
                <% listData.forEach((rooms)=> { %>
                    <div class="col-md-4 mb-3">
                        <div class="card">
                            <img src="../../<%= rooms.photos_room %>" class="card-img-top" alt="Image">
                            <div class="card-body">
                                <h5 class="card-title">
                                    <%= rooms.room_type %>
                                </h5>
                                <p class="card-text"><strong>ID Room:</strong>
                                    <%= rooms._id %>
                                </p>
                                <p class="card-text"><strong>ID Người tạo:</strong>
                                    <%= rooms.landlord_id %>
                                </p>
                                <p class="card-text"><strong>Giá:</strong>
                                    <%= rooms.price %>
                                </p>
                                <p class="card-text"><strong>Kích thước:</strong>
                                    <%= rooms.size %>
                                </p>
                                <p class="card-text"><strong>Trạng thái:</strong>
                                    <%= rooms.availability_status %>
                                </p>
                                <div class="d-flex justify-content-between">
                                    <a href="/api/post/update/<%= rooms._id %>" class="btn btn-warning">Sửa</a>
                                    <a href="/api/post/delete/<%= rooms._id %>" class="btn btn-danger"
                                        onclick="return confirm('Bạn có chắc muốn xóa sản phẩm này?')">Xóa</a>
                                </div>
                            </div>
                        </div>
                    </div>
                    <% }) %>
            </div>
        </div>

        <%- include('../inc/footer') %>
</body>

</html>